spring:
    profiles:
        active: dev

    datasource:
        driver-class-name: org.h2.Driver
        username: sa
        password:
        url: jdbc:h2:mem:testdb

management:
    endpoints:
        web:
            exposure:
                include: health,info,metrics,prometheus

